#include <iostream>
#include "follower_robot.h"

using namespace enviro;

// Note : All implementation is in follower_robot.h, but the Makefile
//        needs a .cc file to know what .so files to make